# 🚀 Полный запуск проекта SkillSwap

## Быстрый старт

Для полного запуска проекта (Next.js + Socket.IO) используйте один из способов:

### Способ 1: Использование скрипта (рекомендуется)

```bash
./start-full.sh
```

Или через npm:

```bash
npm run start:full
# или
npm run dev:full
```

### Способ 2: Остановка серверов

```bash
./stop.sh
```

Или через npm:

```bash
npm run stop
```

---

## Что делает скрипт `start-full.sh`?

1. ✅ **Проверяет наличие `.env.local`**
   - Если файл не найден, показывает инструкцию по созданию

2. ✅ **Устанавливает зависимости**
   - Проверяет наличие `node_modules` в корне проекта
   - Устанавливает зависимости, если нужно
   - Проверяет и устанавливает зависимости для `socket-server`

3. ✅ **Генерирует Prisma Client**
   - Генерирует Prisma Client для основного проекта
   - Генерирует Prisma Client для socket-server

4. ✅ **Подготавливает конфигурацию**
   - Копирует `.env.local` в `socket-server/.env` (если нужно)

5. ✅ **Запускает серверы**
   - Запускает Socket.IO сервер на порту 3001 (в фоновом режиме)
   - Запускает Next.js сервер на порту 3000 (в фоновом режиме)

6. ✅ **Показывает информацию**
   - Выводит URL серверов
   - Показывает команды для просмотра логов
   - Обрабатывает Ctrl+C для корректного завершения

---

## Просмотр логов

Во время работы серверы пишут логи в файлы:

```bash
# Логи Next.js сервера
tail -f nextjs-server.log

# Логи Socket.IO сервера
tail -f socket-server.log

# Все логи одновременно
tail -f nextjs-server.log socket-server.log
```

---

## Ручной запуск (альтернатива)

Если хотите запустить серверы вручную в разных терминалах:

### Терминал 1: Next.js сервер
```bash
npm run dev
```

### Терминал 2: Socket.IO сервер
```bash
cd socket-server
node index.js
```

---

## Устранение проблем

### Порт уже занят

Если порт 3000 или 3001 уже занят:

```bash
# Остановить все процессы Next.js
pkill -f "next dev"

# Остановить все процессы Socket.IO
pkill -f "node.*socket-server"
```

Или используйте скрипт:

```bash
./stop.sh
```

### Ошибка подключения к базе данных

Убедитесь, что:
1. PostgreSQL запущен
2. База данных `skillswap` создана
3. В `.env.local` указаны правильные данные подключения

### Ошибка генерации Prisma Client

```bash
# Перегенерировать Prisma Client
npm run db:generate

# Для socket-server
cd socket-server
npx prisma generate --schema=../prisma/schema.prisma
cd ..
```

---

## Структура проекта

```
SkillSwapWeb/
├── start-full.sh          # Скрипт полного запуска
├── stop.sh                # Скрипт остановки
├── .env.local             # Конфигурация (не коммитится)
├── socket-server/
│   ├── .env               # Конфигурация socket-server
│   ├── index.js           # Socket.IO сервер
│   └── start.sh           # Скрипт запуска socket-server
├── nextjs-server.log      # Логи Next.js (создается автоматически)
└── socket-server.log      # Логи Socket.IO (создается автоматически)
```

---

## Полезные команды

```bash
# Полный запуск
./start-full.sh

# Остановка
./stop.sh

# Просмотр логов Next.js
tail -f nextjs-server.log

# Просмотр логов Socket.IO
tail -f socket-server.log

# Проверка запущенных процессов
ps aux | grep -E "next|socket-server"

# Очистка логов
rm -f nextjs-server.log socket-server.log
```

---

## Автоматический запуск при старте системы

Если хотите, чтобы проект запускался автоматически:

### macOS (launchd)

Создайте файл `~/Library/LaunchAgents/com.skillswap.plist`:

```xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>Label</key>
    <string>com.skillswap</string>
    <key>ProgramArguments</key>
    <array>
        <string>/bin/bash</string>
        <string>/Users/YOUR_USERNAME/Documents/SkillSwapWeb/start-full.sh</string>
    </array>
    <key>RunAtLoad</key>
    <true/>
    <key>KeepAlive</key>
    <true/>
    <key>WorkingDirectory</key>
    <string>/Users/YOUR_USERNAME/Documents/SkillSwapWeb</string>
</dict>
</plist>
```

Загрузите:

```bash
launchctl load ~/Library/LaunchAgents/com.skillswap.plist
```

---

## Готово! 🎉

После запуска:
- 🌐 Next.js: http://localhost:3000
- 🔌 Socket.IO: http://localhost:3001
